# TMK 同声传译 — 未来规划

## 1. 紧急修复（上线前）

| # | 优先级 | 问题 | 位置 |
|---|---|---|---|
| 1 | P0 | translate 接口吞错误，失败也返回 200 | `server.go:162` 丢弃 error |
| 2 | P0 | WebSocket `"audio"` text 消息发 JSON 而非 PCM | `server.go:383` audio case |
| 3 | P0 | StartInterpret TOCTOU 竞态，Stop 被吞 | `sessionservice.go:82-103` 锁范围 |
| 4 | P0 | handlePause/handleStop 缺 try/catch，失败 UI 冻结 | `App.tsx:123,133` |

## 2. 高优先级（v1.1）

| # | 功能 | 说明 |
|---|---|---|
| 1 | 翻译错误降级 | 翻译失败返回原文 + warning 而非静默 |
| 2 | ASR WebSocket 并发写 | `bailian.go` 加 write mutex |
| 3 | WASAPI Stop 死锁 | `wasapi.go:126-146` 加超时保护 |
| 4 | DB 写入错误处理 | `session.go` 所有 Exec 检查 error |
| 5 | 优雅退出 | `main.go` `Close()` 改为 `Shutdown()` |
| 6 | 服务端语言校验 | 创建会话时验证 lang code |
| 7 | 云端配置下发 | 后端地址从 config 文件读取，不硬编码 |

## 3. 功能迭代（v1.2）

| # | 功能 | 说明 |
|---|---|---|
| 1 | 桌面悬挂字幕 | 双窗口架构：frameless + AlwaysOnTop + 透明背景 |
| 2 | 音频设备真实枚举 | 替换 `api/v1/audio/devices` 硬编码 stub |
| 3 | 用户设置持久化 | 语言偏好、设备选择存本地 |
| 4 | 快捷键控制 | 全局热键 启动/暂停/停止 翻译 |
| 5 | 系统托盘 | 最小化到托盘，托盘图标控制 |

## 4. 体验优化（v1.3）

| # | 功能 | 说明 |
|---|---|---|
| 1 | TTS 语音合成 | 翻译结果朗读，多音色选择 |
| 2 | 界面美化 | 暗色主题优化、动画过渡 |
| 3 | 翻译记录导出 | 导出为 TXT/SRT 字幕文件 |
| 4 | 历史记录搜索 | 按关键词/日期搜索历史翻译 |
| 5 | 历史记录删除 | 单条/批量删除，API + UI |

## 5. 架构升级（v2.0）

| # | 方向 | 说明 |
|---|---|---|
| 1 | MySQL 替代 SQLite | 多用户场景，API 文档 DDL 已就绪 |
| 2 | 用户认证 | API Key 管理，多租户隔离 |
| 3 | Web 管理后台 | 非桌面场景的 Web 端 |
| 4 | 多平台发布 | macOS 适配（Wails 已支持） |
| 5 | CI/CD | GitHub Actions 自动构建+发布 |
| 6 | 测试覆盖 | Store/Handler 单元测试 + 集成测试 |

## 6. 技术债

| # | 项 | 说明 |
|---|---|---|
| 1 | 零测试覆盖 | 需补 Store、Handler、前端组件测试 |
| 2 | go.mod 版本清理 | 确认依赖版本，移除未使用依赖 |
| 3 | 死代码清理 | GreetService、SendAudio TS binding、TTS 占位 |
| 4 | 日志规范 | 统一日志级别，区分 dev/prod |
| 5 | 错误码标准化 | 对齐 API 文档错误码表 |
| 6 | 前端接口收敛 | 统一走 Wails 绑定，不混用 fetch() |
